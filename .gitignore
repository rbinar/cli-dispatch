.config/
config
.env
*.local

# Internal design notes — local only, not published
.specs/

# vhs demo source — local only; the rendered assets/demo.gif is what ships
*.tape

# macOS cruft
.DS_Store

# Playwright MCP QC artifacts (transient)
.playwright-mcp/
dashboard-*.png
