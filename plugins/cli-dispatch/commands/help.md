---
description: One-screen command reference for cli-dispatch
allowed-tools: Bash
---

!`bash "${CLAUDE_PLUGIN_ROOT}/scripts/cli-dispatch-help.sh"`

The reference above already ran — do NOT run it again and do NOT retype it.

Show it to the user verbatim, inside a code fence so the box borders line up.
Add nothing after it unless the user asked about a specific command.
